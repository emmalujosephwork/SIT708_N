<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center">

    <!-- Email input field with custom style and margin -->
    <EditText
        android:id="@+id/username"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:layout_centerHorizontal="true"
        android:background="@drawable/edit_text_background"
        android:padding="15dp"
        android:textColor="#000000"
        android:layout_marginBottom="15dp" /> <!-- Adds gap below the field -->

    <!-- Password input field with custom style and margin -->
    <EditText
        android:id="@+id/password"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/username"
        android:hint="Password"
        android:inputType="textPassword"
        android:layout_centerHorizontal="true"
        android:background="@drawable/edit_text_background"
        android:padding="15dp"
        android:textColor="#000000"
        android:layout_marginBottom="15dp" /> <!-- Adds gap below the field -->

    <!-- Log in button with custom style and margin -->
    <Button
        android:id="@+id/login_button"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/password"
        android:text="Log in"
        android:layout_centerHorizontal="true"
        android:background="@drawable/button_background"
        android:textColor="#FFFFFF"
        android:textAppearance="@style/CustomTextStyle"
        android:layout_marginTop="20dp" /> <!-- Adds gap above the button -->

    <!-- Sign Up button with custom style and margin -->
    <Button
        android:id="@+id/signup_button"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_button"
        android:text="Sign Up"
        android:layout_centerHorizontal="true"
        android:background="@drawable/button_background"
        android:textColor="#FFFFFF"
        android:textAppearance="@style/CustomTextStyle"
        android:layout_marginTop="10dp" /> <!-- Adds gap above the button -->
</RelativeLayout>
