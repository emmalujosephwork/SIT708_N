<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:padding="16dp">

    <!-- Full Name EditText with custom style and margin -->
    <EditText
        android:id="@+id/full_name"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:hint="Full Name"
        android:inputType="text"
        android:layout_centerHorizontal="true"
        android:background="@drawable/edit_text_background"
        android:padding="15dp"
        android:textColor="#000000"
        android:layout_marginBottom="15dp" /> <!-- Adds gap below the field -->

    <!-- Username EditText with custom style and margin -->
    <EditText
        android:id="@+id/username"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/full_name"
        android:hint="Username"
        android:inputType="text"
        android:layout_centerHorizontal="true"
        android:background="@drawable/edit_text_background"
        android:padding="15dp"
        android:textColor="#000000"
        android:layout_marginBottom="15dp" /> <!-- Adds gap below the field -->

    <!-- Password EditText with custom style and margin -->
    <EditText
        android:id="@+id/password"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/username"
        android:hint="Password"
        android:inputType="textPassword"
        android:layout_centerHorizontal="true"
        android:background="@drawable/edit_text_background"
        android:padding="15dp"
        android:textColor="#000000"
        android:layout_marginBottom="15dp" /> <!-- Adds gap below the field -->

    <!-- Confirm Password EditText with custom style and margin -->
    <EditText
        android:id="@+id/confirm_password"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/password"
        android:hint="Confirm Password"
        android:inputType="textPassword"
        android:layout_centerHorizontal="true"
        android:background="@drawable/edit_text_background"
        android:padding="15dp"
        android:textColor="#000000"
        android:layout_marginBottom="15dp" /> <!-- Adds gap below the field -->

    <!-- Create Account Button with custom style and margin -->
    <Button
        android:id="@+id/create_account_button"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/confirm_password"
        android:text="Create Account"
        android:layout_centerHorizontal="true"
        android:background="@drawable/button_background"
        android:textColor="#FFFFFF"
        android:textAppearance="@style/CustomTextStyle"
        android:layout_marginTop="20dp" /> <!-- Adds gap above the button -->

</RelativeLayout>
